{
  "name": "ibs-lms-functions",
  "private": true,
  "engines": {
    "node": "18"
  },
  "dependencies": {
    "firebase-admin": "^13.6.0",
    "firebase-functions": "^7.0.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  },
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "lint": "echo 'TODO: add lint'",
    "serve": "firebase emulators:start --only functions"
  }
}
